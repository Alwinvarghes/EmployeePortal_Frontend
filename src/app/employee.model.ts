



//instead of create this variable we are creating one modeel and impoert that model in
//all component that model in all component that need this model.

export class EmployeeModel{
    id?:string
    username?:string
    email?:string
    status?:string
}